{
  "name": "badger-config-ui",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/BadgerConfigUI-1.0",
  "tags": ["type:lib"],
  "targets": {
    "format": {
      "executor": "nx:run-commands",
      "options": { "command": "stylua libs/BadgerConfigUI-1.0" }
    },
    "format-check": {
      "executor": "nx:run-commands",
      "options": { "command": "stylua --check libs/BadgerConfigUI-1.0" }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "luacheck libs/BadgerConfigUI-1.0" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "busted libs/BadgerConfigUI-1.0" }
    }
  }
}
