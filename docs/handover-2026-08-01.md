---
title: Session handover — 2026-08-01
type: handover
related:
  - docs/decisions.md
  - docs/workorders.md
  - docs/reference/estimator-scoreboard.json
---

# Session handover — 2026-08-01

Where **badger-ttk** stands after this session, what is genuinely blocking, and how to pick up cold.
Canonical state is always [`docs/decisions.md`](decisions.md) *Current state* — this file is the narrative
version plus the loose threads a fresh session would otherwise have to rediscover.

## The one-line summary

**0.9.48 shipped the regime layer; the only thing standing between the addon and 1.0.0 is a human `/reload`
in the live client.** Everything else on the Path-B 1.0.0 list is done.

**Addendum (second session, 2026-08-01).** The PR #97 verification thread is **closed** — re-run clean, 5
surviving findings, none fixed yet, one deliberately parked. The terminal MCP is **fixed in config** and
needs one human approval. Nothing regressed: `pnpm validate --skip-nx-cache` is green at `71273d7`. The
`/reload` is still the only 1.0.0 gate; the verification findings are **not** gates — they are the next
work order.

## What shipped this session

| release | what |
|---|---|
| [0.9.46](https://github.com/igorszyporynjorgensen-sketch/badger-addons/releases/tag/badger-ttk/0.9.46) | kill history records full Warcraft-Logs-shaped kills (WO-072, D-012 schema) |
| [0.9.47](https://github.com/igorszyporynjorgensen-sketch/badger-addons/releases/tag/badger-ttk/0.9.47) | **Onyxia** rhythm profile (WO-074) — the last missing raid boss |
| [0.9.48](https://github.com/igorszyporynjorgensen-sketch/badger-addons/releases/tag/badger-ttk/0.9.48) | **the regime layer** (WO-075, D-014/D-018) — the bar goes quiet instead of guessing |

Plus the **release pipeline itself** (WO-073, D-017): `nx release`, independent per-addon versioning off each
`.toc`, conventional-commits bump + changelog, tag `{projectName}/{version}`, zip, GitHub Release. The old
`v0.9.44`/`v0.9.45` tags were retrofitted to the new scheme, so tag history is uniform.

## The regime layer in one paragraph

An injected, read-only per-encounter table (`opts.regime`) consumed by **nil-guarded seams inside
`estimator.lua`**; with `regime == nil` the estimator is **byte-identical** to before, which is enforced by
diffing `tools/estimator-sim.lua` against `main` on every PR. Profiles live in
`src/raids/regimes.lua` — **auto-generated**, never hand-edited. The confidence caps that make the bar go
quiet are **learned from the estimator's own measured per-bin error** over ~900 real kills, so "this fight is
unreadable" is a measurement, not an opinion. Headline: **Buru 188.6%→37.3%**, Twin Emperors 407%→**silent**,
and two bosses (Chromaggus, Ouro) measured as readable correctly got **no caps at all**.

## The lab (all client-side, never in CI)

```sh
export PATH="$HOME/.luarocks/bin:$PATH"          # REQUIRED — luacheck/busted/luajit live here
python3 tools/wcl-corpus.py <freshEncId> --n 60  # harvest real kills  (Fresh id = classic id + 150000)
python3 tools/learn-regime.py <id> --split-even  # learn caps from measured error (+ --suppress-flush)
python3 tools/assemble-regimes.py                # candidates + hand facts -> src/raids/regimes.lua
python3 tools/grade-regimes.py                   # held-out baseline vs SHIPPED, MAPE *and* shown%
luajit  tools/estimator-batch.lua <dir> --shipped # grade what actually ships
pnpm validate                                    # the gate (stylua · luacheck · busted)
```

`tools/fights/corpus/` is **gitignored and disposable** (~920 fixtures right now — Onyxia ×200 plus 12 bosses
×60). The WCL API key is in a gitignored `.env`. Never run harvesting/learning in CI.

## Blocking, and only this

**The in-game `/reload`** — a real client check, not a session restart. It gates 1.0.0 *and* every CurseForge
upload (D-015); 0.9.46/0.9.47/0.9.48 are on GitHub but **not** on CurseForge. There is now a copy-pasteable
protocol for it: **[`docs/reference/in-game-verification.md`](reference/in-game-verification.md)** — Pass A
(solo, ~5 min) is the actual 1.0.0 gate; Pass B (in a raid) confirms the regime layer. In short it confirms:

1. the addon loads clean on the live Classic Era client;
2. **which encounter id-space the client actually fires** — `663` (classic) or `150663` (Fresh). Profiles are
   dual-keyed so either resolves, but this is still unconfirmed in-game and worth checking with
   `/dump` on `ENCOUNTER_START`;
3. the `.toc` `## Interface: 11509` matches the live build (`/run print((select(4, GetBuildInfo())))`);
4. bars track a real target; and
5. the new quiet behaviour looks right — **Majordomo shows no bar**, and Twins/Thekal stay silent.

## Open threads (nothing is on fire)

- **Adversarial verification of PR [#97](https://github.com/igorszyporynjorgensen-sketch/badger-addons/pull/97)
  — DONE, re-run clean. Full record: [`docs/reference/pr97-verification-2026-08-01.md`](reference/pr97-verification-2026-08-01.md).**
  The stale-tree worry was handled by re-running from scratch against HEAD `71273d7` under a hard read-only
  git rule (scratchpad copies for repro; `git status` confirmed the repo untouched afterwards). Five lenses
  → adversarial refuters → a completeness critic → a second three-lens round on the critic's top finding.
  **32 agents, 0 errors, 18 candidates → 6 confirmed + 1 unrefuted. Nothing fixed yet.**

  **The headline is not any of the things the sweep was pointed at.** It is that **the lab does not model
  the client**, on three separate axes — and the biggest one is confirmed by three independent
  reproductions:

  - **The show gate is STICKY and the lab never modelled it.** `driver.lua:67` checks confidence only while
    `not wasShown`; once the bar latches in any health bin, every later `confCap` — including a hard `0.0` —
    is unreachable. `estimator.lua:245-246` says so in a comment; `estimator-batch.lua:110` and
    `learn-regime.py:207-208` both assume the opposite. Consequence: **Viscidus's profile is bit-identical
    to shipping no regime at all**, Skeram and Sulfuron are ~nullified, Onyxia and Buru are degraded.
    Gahz'ranka (PR97's own boss) is unaffected, so **PR #97's own claim stands**, and Buru's
    188.6%→37.3% survives because that win is `suppressFlush`, not caps. The Viscidus/Skeram/Sulfuron cap
    numbers describe a client that does not exist.
  - **4 more are safe to fix off-client** and gate-provable: the per-bin `confCap` path has **no test that
    it ever caps** (two breaking mutations survive all 151 tests); `resetOnRise`'s magnitude threshold is
    untested; `assemble-regimes.py` emits a profile shape `regimes_spec` rejects — **already decided by
    D-019(b): the spec is wrong, not the generator**; and the scalar-collapse path discards measured zeros.
  - **1 is PARKED — see [D-021-IJ].** `resetOnRise` may never fire live (the `damageable = not dead` hold
    swallows the resurrect; 61 lab firings vs 5 live). Affects one or very few bosses, **cannot be tested
    without a real ZG kill**. **Do not fix it blind.**
  - **The critic found the question that outranks everything**: `regimeFor` engages only when
    `UnitLevel("target") == -1`, and **nobody has ever verified that on a live instanced Era boss**. If it
    reports a number, the entire regime layer has been inert for every player since release. One `/run`
    line settles it — protocol step **B5**, now first in Pass B.
  - Eleven refuted candidates are recorded in the findings doc too, so no future sweep re-spends tokens.
- **iTerm2 MCP — RESOLVED in config, still needs one approval.** The original `npx iterm_mcp_server` setup
  never ran, and **the restart was not the reason**: project-scoped `.mcp.json` servers sit behind an
  approval gate, and `~/.claude.json` for this project had `enabledMcpjsonServers: []` with
  `hasTrustDialogAccepted: false`. Claude Code never *attempted* the server — there was no
  `mcp-logs-terminal` directory beside `mcp-logs-plugin-figma-figma` in
  `~/Library/Caches/claude-cli-nodejs/-Users-scandesign-PetProjects-badger-addons/`. That triad
  (no tools · no log dir · empty `enabledMcpjsonServers`) is the fast way to diagnose this again.

  `iterm_mcp_server` was also the **wrong package** for the goal. Its `execute-command` writes to a hidden
  background shell *and* separately to whatever iTerm window is frontmost, while `read-output` reads **only
  the hidden shell** — so what appears on screen and what the agent reads back come from two different
  shells whose `cwd`/`PATH`/exports diverge immediately. It also mangles quoted commands (shell-style
  escaping applied to AppleScript string content) and steals focus on every call.

  **Now:** `iterm_mcp_server` uninstalled; **`iterm-mcp@1.2.6`** (ferrislucas) installed globally on node 22;
  `.mcp.json` points at the absolute binary (`~/.nvm/versions/node/v22.22.3/bin/iterm-mcp`) rather than
  `npx` — `npx` ignores global installs and, on a cache miss, prompts for confirmation with no TTY under MCP
  stdio, which can hang the server. Handshake verified by hand: it exposes `write_to_terminal`,
  `read_terminal_output`, `send_control_character`, all against the **single active iTerm session**.
  **Remaining step is human-only: approve the project MCP server at the startup prompt.** Writing
  `enabledMcpjsonServers` by hand would forge that approval, so it was deliberately left alone.
  Fallback if it still misbehaves: `tools/ttk-lab.py`, the purpose-built live ANSI dashboard.
- **Scaffold-project back-port** of the release pipeline: unblocked (real cycles have run) but **on hold** by
  explicit instruction.
- **Naxxramas** has no rhythm/regime profiles — no Fresh log corpus existed. Learnable the moment one does.
- **The CLEU off-target tier** (heal add-back for Sulfuron/Jin'do, Majordomo's add counter, the Twins' second
  pool) is designed and deferred; reserved schema slots (`healPolluted`, `secondPool`) already ship inert.
  This is the tier that could make today's *silent* bosses estimable.
- **Next ids:** `D-021-IJ`, `WO-076-IJ`.

## First prompt for the next session (copy-paste)

> **Branch note.** These docs live on `docs/D-021-IJ-pr97-verification`
> (PR [#99](https://github.com/igorszyporynjorgensen-sketch/badger-addons/pull/99)), **not yet on `main`**.
> The session was left on that branch on purpose — checking out `main` before the PR merges makes this file
> and the findings doc vanish from the working tree. After merging, `git checkout main && git pull`.

```text
Picking up badger-ttk. You'll be on branch docs/D-021-IJ-pr97-verification — that's
deliberate; PR #99 carries these docs and main doesn't have them yet. If I've already merged
it, switch to main and pull first.

Read docs/handover-2026-08-01.md first, then
docs/reference/pr97-verification-2026-08-01.md ("Read this first" section), then
docs/decisions.md "Current state". Those are canonical.

Short version: 0.9.48 shipped the regime layer. The PR #97 verification is DONE — 32 agents,
6 confirmed findings. The big one is that THE LAB DOES NOT MODEL THE CLIENT: the driver's
show gate is sticky (confidence is only checked before the bar's first appearance), but the
grader and the learner both assume a cap can hide the bar in any bin. Result: Viscidus's
shipped profile is bit-identical to having no regime at all, Skeram and Sulfuron are
~nullified. No player sees a wrong number — the bar just stays up where we thought it went
quiet. The fix is instrument-side and cannot regress the addon.

A real in-game /reload is still the only 1.0.0 gate. I still haven't done it.

Do these in order:

1. Confirm the terminal MCP is live. I approved it at startup this time. You should have
   write_to_terminal / read_terminal_output / send_control_character from the `terminal`
   server (iterm-mcp@1.2.6, absolute path in .mcp.json). If they're NOT there, say so
   plainly — don't work around it silently. Diagnosis triad: no tools + no mcp-logs-terminal
   dir in ~/Library/Caches/claude-cli-nodejs/-Users-scandesign-PetProjects-badger-addons/ +
   empty enabledMcpjsonServers in ~/.claude.json. Once live, run lab/learning commands in MY
   terminal so I can watch them.

2. Propose WO-076-IJ: make the lab model the client. Instrument-only, so it cannot regress
   the addon:
     - teach estimator-batch.lua / estimator-perbin.lua the sticky gate (wasShown)
     - fix learn-regime.py:207-208's per-bin independent-hide assumption
     - pass damageable = not dead instead of hard-coding true
     - re-learn + re-grade, and show me the diff against the shipped caps
     - fix regimes_spec's "empty profile" test to accept a kills-only profile (D-019(b)
       already decided this — the spec is wrong, not the generator)
   Propose it, wait for me to set it Accepted, then branch + PR. I merge, you never do.

3. WO-077-IJ (can go first if you prefer, it's smaller): the two test holes — the per-bin
   confCap path has no test that it ever caps, and resetOnRise's magnitude threshold is
   untested. Both are pure spec additions.

4. DO NOT touch finding 5 (Thekal resetOnRise / the damageable hold) — parked by D-021-IJ.
   Can't be tested without a real ZG kill; the suspect code is on every boss's path and a
   blind change risks the regime==nil byte-identity guarantee. WO-076's instrument fix may
   dissolve it on its own. Also don't act on finding 7 — it never got an adversarial pass.

5. When I next log in, remind me to run protocol step B5 from
   docs/reference/in-game-verification.md: one /run line checking UnitLevel("target") == -1.
   If an instanced Era boss reports a number instead of -1, regimeFor never fires and the
   whole regime layer has been inert for every player since release. That outranks
   everything above.

Reminders: PATH="$HOME/.luarocks/bin:$PATH" for the gate, and --skip-nx-cache when the answer
matters (a cached green gate means nothing ran — it bit us). tools/fights/corpus/ is
disposable. Learning is client-side only, never CI. No version bump without a deliberate
release. Link PR numbers as clickable URLs. Don't run big multi-agent sweeps unless I ask.
```

## Three lessons this session paid for (don't re-learn them)

1. **Measure the thing itself, not a proxy.** Caps were nearly derived from the dispersion of remaining time;
   its denominator vanishes as a fight ends, so it would have silenced the **endgame** — the stretch the bar
   gets *most* right (~3s error with 10s to live). Measuring the estimator's own error has no such artifact.
2. **A blanket default must not override a measurement.** Folding the universal cap into a *measured* boss
   silenced Chromaggus' most readable band and made his grade worse. Defaults are for the un-measured.
3. **Learn under the configuration that ships.** Caps derived without a boss's own categorical facts are
   calibrated against an estimator that never runs (Buru ~3× worse, Thekal ~1.5×) and over-silence a usable
   readout.

## Working agreement reminders (they bit during this session)

- Docs under `docs/` are free to edit; **everything else needs explicit acceptance**, lands on a branch, and
  reaches `main` only via a PR **the human merges**. The AI never merges.
- **Work-order files live-mirror to `main`** (no branch/PR); the *code* they drive does not.
- A version bump is a **deliberate release act**; conventional commits only decide *how much* to bump.
- On 0.x, Nx adjusts a `minor` bump downward — which is why `feat:` commits landed on **0.9.48**, not 0.10.0.
